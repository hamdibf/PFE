{% extends "cnctfacpBundle::layout.html.twig" %}

{% block title %}
	Administration - {{ parent() }}
{% endblock %}
{% block menu %}
        <h1><font color="#800080">Administration</font></h1>
{% endblock %}
{% block facp_body %}
<h1>Liste des sous_directions</h1>

<table class="table table-striped table-bordered table-condensed">
    <thead>
        <tr>
            <th>Id</th>
            <th>Code_s_dir</th>
            <th>Libelle</th>
            <th>Direction</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('sous_direction_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.codesdir }}</td>
            <td>{{ entity.libelle }}</td>
            <td>{{ entity.direction }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('sous_direction_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('sous_direction_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('sous_direction_new') }}">
            Créer une nouvelle sous direction
        </a>
    </li>
</ul>
{% endblock %}